<h1>Listing playlists</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>User</th>
    <th>Favorite count</th>
    <th>Flags</th>
    <th>Status</th>
  </tr>

<% @playlists.each do |playlist| %>
  <tr>
    <td><%=h playlist.title %></td>
    <td><%=h playlist.description %></td>
    <td><%=h playlist.user_id %></td>
    <td><%=h playlist.favorite_count %></td>
    <td><%=h playlist.flags %></td>
    <td><%=h playlist.status %></td>
    <td><%= link_to 'Show', playlist %></td>
    <td><%= link_to 'Edit', edit_playlist_path(playlist) %></td>
    <td><%= link_to 'Destroy', playlist, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New playlist', new_playlist_path %>